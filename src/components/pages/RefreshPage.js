import React from 'react';
import RefreshSection from './sections/RefreshSection';

const RefreshPage = () => {
  return (
    <React.Fragment>
      <RefreshSection />
    </React.Fragment>
  );
};

export default RefreshPage;
